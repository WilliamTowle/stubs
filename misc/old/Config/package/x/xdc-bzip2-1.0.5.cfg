# bzip2 v1.0.5			[ since v?.?, c.????-??-?? ]
# last mod WmT, 2010-03-11	[ (c) and GPLv2 1999-2010 ]

PKG_SRC=${SRCDIR}/b/bzip2-${PKG_VER}.tar.gz

PKG_URLS=http://www.bzip.org/${BZIP2_VER}/bzip2-${PKG_VER}.tar.gz

# [ "${BUILD_NPI}" = 'y' ] && DEPS="libc ldso"
[ "${BUILD_CUI}" = 'y' ] && DEPS="xdc-uClibc xdc-diffutils"

#[ "${BUILD_NTI}" = 'y' ] && METHOD=sh
[ "${BUILD_CUI}" = 'y' ] && METHOD=mak

